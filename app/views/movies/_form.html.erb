<%= form_with(id: dom_id(movie), model: movie, local: false) do |form| %>
  <% if movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div id="<%= dom_id(movie) %>_title_form_<%=@attribute%>" class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div id="<%= dom_id(movie) %>_description_form_<%=@attribute%>" class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div id="<%= dom_id(movie) %>_duration_form_<%=@attribute%>" class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration %>
  </div>

  <div id="<%= dom_id(movie) %>_image_form_<%=@attribute%>" class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div id="<%= dom_id(movie) %>_year_form_<%=@attribute%>" class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div id="<%= dom_id(movie) %>_director_form_<%=@attribute%>" class="field">
    <%= form.label :director_id %>
    <%= form.text_field :director_id %>
  </div>

  <div id="<%= dom_id(movie) %>_submit_form_<%=@attribute%>" class="actions">
    <%= form.submit %>
  </div>
<% end %>
